extends layout

block content
  form(method='POST')
    legend Forgot Password
    .form-group
      label(for='identifier') Email
      input.form-control(type='text', name='identifier', autofocus, required)
    button.btn.btn-primary(type='submit') Reset Password